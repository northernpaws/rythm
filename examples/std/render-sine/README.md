# render-sine

This is a basic example using the Rust std library that renders a sine wave oscillator turned to middle C to a wav file. This serves as a bare basic example of how to use the oscillators. 